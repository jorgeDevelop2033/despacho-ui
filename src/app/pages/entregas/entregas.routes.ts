import { Routes } from '@angular/router';
import { CrearEntregaComponent } from './crear-entrega.component';

export const ENTREGAS_ROUTES: Routes = [
  { path: '', component: CrearEntregaComponent }
];
